# Big Break Backend

Backend v1 для Big Break.

Стек:
- NestJS
- Prisma
- PostgreSQL
- Redis
- Cloud.ru Object Storage
- raw WebSocket

Локальный запуск:

```bash
docker compose --env-file ../.env.production.local up --build --remove-orphans
```
